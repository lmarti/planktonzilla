# @package _global_

# to execute this experiment run:
# python train.py experiment=base_zoolake model=...
defaults:
  - default.yaml
  - override /datamodule: zooscan.yaml
  - override /transformation: zooscan.yaml
