# @package _global_

# specify here default configuration
# order of defaults determines the order in which configs override each other
defaults:
  - _self_
  - paths: default.yaml
  - extras: default.yaml
  - hydra: default.yaml
  - dataset_import: isiisnet.yaml
  - tracking: default.yaml


task_name: "import_dataset"
tags: ["dev"]

action: show